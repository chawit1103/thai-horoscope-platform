{
  "name": "thai-horoscope-platform",
  "private": true,
  "packageManager": "pnpm@10.33.2",
  "scripts": {
    "beta:dry-run": "pnpm --filter @thai-horoscope/web beta:dry-run",
    "dev": "pnpm --filter @thai-horoscope/web dev",
    "lint": "pnpm -r --if-present lint",
    "typecheck": "pnpm -r --if-present typecheck",
    "test": "pnpm -r --if-present test"
  },
  "devDependencies": {
    "@types/node": "^25.6.0",
    "tsx": "^4.21.0",
    "typescript": "^5.8.3"
  }
}
